<li>
  <%= gravatar_for user, size: Settings.avatar_size %>
  <%= link_to user.name, user %>
  <% if check_user? user %>
    <span>|</span>
    <%= link_to t("users.index.delete"), user, method: :delete, data: {confirm: t("users.index.comfirm")} %>
  <% end %>
</li>
